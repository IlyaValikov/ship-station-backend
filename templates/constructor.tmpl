<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ShipStation</title>
    <!-- Подключение Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/index.css" />
  </head>
  <body>
    <!-- Навигационная панель (navbar) -->
    <nav class="navbar">
      <!-- Иконка слева -->
      <a class="navbar-brand" href="/">
        <div class="brand-container">
          <img
            src="/images/ShipStation.png"
            width="34"
            height="34"
            class="icon"
            alt="Icon"
          />
          <h1 class="brand-name">ShipStation</h1>
        </div>
      </a>
    </nav>
    <div class="brand-container">
      <h2 class="brand-name">Конструктор заявки: {{ len .ships }}</h2>
    </div>
    <!-- Список багажей в виде карточек -->
    <div class="container">
      <!-- Обертка для строк с карточками -->
      <div class="row">
        {{ range.ships }}
        <div class="col-md-4">
          <div class="cards mb-4">
            <a href="/ship/{{.ShipID}}" class="card-link">
              <!-- Добавляем класс card-link -->
              <img
                src="{{.Photo}}"
                class="cards-img-top"
                alt="Фотография судна"
              />
              <div class="cards-body">
                <h5 class="cards-title">{{.ShipName}}</h5>
              </div>
            </a>
          </div>
        </div>
        {{ end }}
      </div>
      <!-- Конец обертки для строк с карточками -->
    </div>
  </body>
</html>
